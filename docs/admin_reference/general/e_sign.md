# `e_sign`
## Electronic Signature

Configure electronic document signing for an activity log extra log type. Requires a paired filestore setup. Supports creating documents on-demand and auto-creating documents when creating the signature activity via a save trigger.

```yaml
!defs(activity_log_options_defs.yaml)
```

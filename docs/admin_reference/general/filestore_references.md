# Filestore `references` — Filestore Container Reference Configuration

The standard references configuration for attaching an NFS filestore container to an activity log. Typically stored once in the Config Library for reuse.

```yaml
!defs(filestore_references_defs.yaml)
```

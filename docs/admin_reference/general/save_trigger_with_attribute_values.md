# `with_attribute_values`
## Map Attribute Values in Trigger Context

Map and transform attribute values from the current record or related models for use within a trigger task. Also used within `preset_fields` — see [preset_fields](../preset_fields.md).

```yaml
!defs(save_triggers_with_attribute_values_defs.yaml)
```

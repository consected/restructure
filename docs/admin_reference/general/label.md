# `label` and `button_label` — Display Labels

`label` sets the main display label for the definition, and is also used as the `+` add button label if `button_label` is not defined. `button_label` overrides the add record button label independently.

```yaml
!defs(extra_options_label_defs.yaml)
```

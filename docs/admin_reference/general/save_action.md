# `save_action`
## Post-Save UI Actions

Configure UI actions to perform after a record is saved or created: showing/hiding panels, refreshing panels, expanding references, redirecting, or returning to the master record.

```yaml
!defs(extra_options_save_action_defs.yaml)
```

# Conditions Reference

Conditions are used throughout the options (in `creatable_if`, `editable_if`, `showable_if`, `valid_if`, `add_reference_if`, save trigger `if:` clauses, etc.) to express conditional logic against model data.

```yaml
!defs(conditions_defs.yaml)
```

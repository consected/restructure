# `fields`
## Displayed Field List

Specify the ordered list of fields to display for this definition or extra log type. Only listed fields will be shown.

```yaml
!defs(extra_options_fields_defs.yaml)
```

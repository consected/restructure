# Filestore `save_trigger` — Filestore Container Creation Trigger

The standard save trigger for creating an NFS filestore container when an activity log record is created. Typically stored once in the Config Library for reuse.

```yaml
!defs(filestore_save_trigger_defs.yaml)
```

# Activity Log Standard Options — Reusable YAML Anchors

Pre-defined YAML anchors specific to activity log configurations, such as checking whether an activity has already been completed.

```yaml
!defs(activity_log_options_standard_option_defs.yaml)
```

# Extra Log Type Definitions

Each key in the activity log options (outside of `default:`) defines a named extra log type. Extra log types represent the specific activities that can be performed within an activity log — each has its own configuration of fields, labels, conditionals, references, and triggers.

```yaml
!defs(activity_log_extra_log_type_defs.yaml)
```

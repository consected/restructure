<%= form_for(@protocol_outcome) do |f| %>
  <% if @protocol_outcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@protocol_outcome.errors.count, "error") %> prohibited this protocol_outcome from being saved:</h2>

      <ul>
      <% @protocol_outcome.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
<div class="field">
    <%= f.label :protocol_id %><br>
    <%= f.select :protocol_id, protocol_array %>
  </div>
    <div class="field">
    <%= f.label :admin_id %><br>
    <%= f.text_field :admin_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

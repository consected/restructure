<% 
  default_options= {
    step: "any",
    data: {
      attr_name: field_name_sym, 
      object_name: form_object_item_type_us
    }
  }

  new_options = default_options.merge( field_options_for(form_object_instance, field_name_sym) )
%>
<%= edit_field_label(form, field_name_sym, labels) %>
<%= form.number_field field_name_sym, new_options %>
<%= form.label field_name_sym, label_for(field_name_sym, labels) %>
<%
  select_item_type = {}
  select_item_type[:selected] = filter_params_permitted.first.last if filter_params_permitted && filter_params_permitted.first.first.to_s == field_name
%>
<%= form.select field_name_sym, send("#{field_name_sym}_options"), select_item_type %>

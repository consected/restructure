  <%  
    begin

    if section == HelpController::IndexSection && subsection ==  HelpController::IndexSubsection
      # Do nothing
    elsif subsection == HelpController::IntroductionDocument
  %>
        <%= link_to 'library home', help_library_home_path(library),
              data: {
                target: '#help-doc-content', 
                toggle: 'scrollto-target'
              }
              %>
    
  <% else %>
    <%= link_to 'back to main section', main_section %>
  <% end %>
  <%
  rescue StandardError
  # Handles missing section / subsection / main_section helper methods cleanly, especially
  # for standalone pages embedded in the sidebar
  end
  %>
<span class="bem-classification-attrs">
<span class="bem-class-title"><%= download.title.present? ? download.title : '(no title)'  %></span>
</span>
<%=link_to "", Rails.application.routes.url_helpers.edit_master_filestore_classification_path(@container.master_id, @container.id, download_id: download.id, retrieval_type: download.class.retrieval_type), class: 'glyphicon glyphicon-pencil', data: {remote: true, container_id: @container_id, toggle: 'scrollto-result'}%>

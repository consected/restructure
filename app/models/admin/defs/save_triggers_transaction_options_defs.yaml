# Save Trigger: Transaction options
#
# Canonical schema
# transaction: <TriggerTaskList>
#
# TriggerTaskList type - see save_trigger.md
# TriggerTaskList: <Array(Hash(trigger_type: TriggerConfig))>
#

# TriggerTaskList type - see save_trigger.md
# TriggerTaskList: <Array(Hash(trigger_type: TriggerConfig))>
#
# Pattern 1: Create related records atomically (save_triggers_transaction_pattern_1_basic_defs.yaml)
# See save_trigger_transaction.md for the worked example and usage notes.

# Notes:
# - All inner triggers run in the same DB transaction as the outer save - an exception in
#   any inner trigger rolls back everything, including the record's own save.
# - Results from all executed triggers are stored in save_trigger_results['transaction']
#   as an array of {trigger:, result:} hashes.
# - Individual triggers within the transaction support on_complete and on_failure
#   lifecycle hooks - see the lifecycle hooks documentation for details.


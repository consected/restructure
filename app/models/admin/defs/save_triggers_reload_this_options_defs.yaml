# Save Trigger: Reload This options
#
# Canonical schema
# reload_this: <Hash(if: Condition, on_complete: TriggerTaskList, on_failure: TriggerTaskList)>
# All keys are optional.
#
# Pattern 1: Reload between two dependent triggers (save_triggers_reload_this_pattern_1_basic_defs.yaml)
# See save_trigger_reload_this.md for the worked example and usage notes.

#!defs(save_triggers_lifecycle_hooks_defs.yaml)

# Notes:
# - reload_this accepts only the universal if/on_complete/on_failure keys - it has no
#   trigger-specific configuration of its own.
# - The trigger preserves current_user and save_trigger_results across the reload.


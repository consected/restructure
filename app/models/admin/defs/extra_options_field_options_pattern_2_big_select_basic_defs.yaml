# Pattern 2: select_record_* field using the big-select modal dialog
field_options:
  item_id:
    edit_as:
      field_type: select_record_from_table_items
      value_attr: id
      label_attr: name
      big_select:
        hide_key: true

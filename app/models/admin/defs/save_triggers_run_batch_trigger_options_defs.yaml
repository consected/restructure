# Save Trigger: Run Batch Trigger options
        run_batch_trigger:
          - run_batch_1:
            if: #if_extras
            resource_name: dynamic_model__view_handle_recs  # Resource name of the dynamic model to run batch trigger for
            mode: foreground  # 'foreground' (default) runs synchronously, 'background' queues as a job
            limit: 100  # Optional limit on number of records to process
          # NOTE: this sets:
          # - save_trigger_results.run_batch_trigger (array of results with resource_name, mode, status, and processed_ids for foreground mode)

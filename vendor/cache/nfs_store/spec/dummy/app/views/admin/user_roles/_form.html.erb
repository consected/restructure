<%= form_for(user_role, local: true, class: 'form') do |form| %>
  <% if user_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_role.errors.count, "error") %> prohibited this user_role from being saved:</h2>

      <ul>
      <% user_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :user_id %>
  <br>
  <%= form.label :role_name %>
  <%= form.text_field :role_name, value: user_role.role_name.present? ? user_role.role_name : 'nfs_store group ' %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
